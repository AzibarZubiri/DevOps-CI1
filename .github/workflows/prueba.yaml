name: CI Prueba

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  list-elements:
    name: List Elements
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - run: ls-l
    
